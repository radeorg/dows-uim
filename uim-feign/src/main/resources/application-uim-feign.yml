#
#feign:
#  okhttp:
#    enabled: true # 开启feign对HttpClient的支持
#    max-connections: 200 # 最大的连接数
#    max-connections-per-route: 50 # 每个路径的最大连接数
#
#  client:
#    config:
#      default: # 可以为所有客户端设置默认配置
#        connectTimeout: 5000
#        readTimeout: 5000
#        loggerLevel: full #
#---
#dows:
#  rpc:
#    uim:
#      url: http://127.0.0.1:28002